public class AppInfo
{
    static int runTime, totalNoPopulation, noThreads, matrixSize, shortesPath, mergerFrequency, noIterations ;
    static float mergerFrequencyFloat;
    static int []threadNoPopulation;
    static PMXThread []PMXProcessingThreads;
    static int [][]matrix;
    static double mutationProbability;
    static boolean killThreads, mergePopulation;

}

